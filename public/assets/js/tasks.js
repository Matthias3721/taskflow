/**
 * Zadania – placeholder pod Fetch API
 */

document.addEventListener('DOMContentLoaded', () => {
    const list = document.getElementById('tasks-list');
    if (!list) return;

    console.info('tasks.js – lista zadań (API w kolejnym etapie)');
});
